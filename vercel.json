{
    "version": 2,
    "buildCommand": "ls -R && cd ./frontend && npm install && npm run build",
    "outputDirectory": "./frontend/dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}